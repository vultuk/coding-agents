interface:
  display_name: "Review Changes"
  short_description: "Run a parallel diff review and adjudicate one final summary"
  default_prompt: "Use $review-changes to analyze the current code diff with multiple reviewer agents in parallel, adjudicate the results into one concise evidence-based markdown review, and use conservative blocker thresholds with high-confidence evidence."
