interface:
  display_name: "Review Changes"
  short_description: "Run six-way diff review with adjudication"
  default_prompt: "Use $review-changes to collect one canonical diff packet, send it to the fixed six-reviewer panel in parallel, adjudicate the results, and return one final review."
