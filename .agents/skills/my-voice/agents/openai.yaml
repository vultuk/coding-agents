interface:
  display_name: "My Voice"
  short_description: "Draft in Simon's channel-specific voice"
  default_prompt: "Use $my-voice to draft this in my voice for review only."

policy:
  allow_implicit_invocation: true
